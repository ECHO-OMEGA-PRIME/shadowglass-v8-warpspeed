# Historical Cloudflare artifact

This directory is provenance-only. It preserves the pre-migration JavaScript,
Wrangler configuration, and package metadata for audit and hash verification.
It is not a deployable entry point: do not run Wrangler or npm deployment
commands from this directory. The maintained FORGE runtime and reversible
cutover tooling live at the repository root.
